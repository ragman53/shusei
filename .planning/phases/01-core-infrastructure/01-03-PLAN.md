---
phase: 01-core-infrastructure
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified: [src/ui/library.rs, src/ui/add_book.rs, src/app.rs]
autonomous: true
requirements: [CORE-01]

must_haves:
  truths:
    - "User sees library screen on app launch"
    - "User can add a book with title and author"
    - "User can optionally add cover photo"
    - "Added book appears in library list"
  artifacts:
    - path: "src/ui/library.rs"
      provides: "Library screen component with book list"
      min_lines: 80
    - path: "src/ui/add_book.rs"
      provides: "Add book modal form component"
      min_lines: 100
    - path: "src/app.rs"
      provides: "Route enum with library routes"
      contains: "enum Route"
  key_links:
    - from: "src/ui/library.rs"
      to: "src/core/db.rs"
      via: "load_books() call"
      pattern: "load_books|get_all_books"
    - from: "src/ui/add_book.rs"
      to: "src/core/db.rs"
      via: "create_book() call"
      pattern: "create_book"
---

<objective>
Build library UI with book list and add book modal form

Purpose: Provide user interface for viewing and adding books
Output: Working library screen and add book form
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-infrastructure/01-CONTEXT.md
@.planning/phases/01-core-infrastructure/01-RESEARCH.md

@src/app.rs
@src/ui/components.rs
@src/core/models.rs
@src/core/db.rs
</context>

<interfaces>
<!-- Key types and contracts from existing code -->

From src/app.rs:
```rust
#[derive(Routable, Clone, PartialEq, Debug)]
enum Route {
    #[layout(HeaderLayout)]
        #[route("/")]
        Home,
    // Extend with BookList, AddBook routes
}
```

From src/core/models.rs (after Plan 01):
```rust
pub struct Book {
    pub id: String,
    pub title: String,
    pub author: String,
    pub cover_path: Option<String>,
    pub pages_captured: i32,
    pub total_pages: Option<i32>,
    pub last_opened_at: Option<i64>,
    pub created_at: i64,
}
```

From src/core/db.rs (after Plan 01-02):
```rust
pub fn get_all_books(&self) -> Result<Vec<Book>>;
pub fn create_book(&self, book: &Book) -> Result<String>;
```
</interfaces>

<tasks>

<task type="auto">
  <name>task 1: Extend Route enum with library routes</name>
  <files>src/app.rs</files>
  <action>Extend existing Route enum in src/app.rs with: #[route("/books")] BookList, #[route("/add-book")] AddBook. Update HeaderLayout if needed to include navigation. Ensure router compiles with new routes.</action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>Route enum extended, app compiles without errors</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: Create LibraryScreen component with book list</name>
  <files>src/ui/library.rs</files>
  <action>Create new file src/ui/library.rs with LibraryScreen component that: 1) Uses use_signal to store Vec<Book>, 2) Uses use_effect to load books on mount via db.get_all_books(), 3) Renders list view with title + author for each book, 4) Shows "No books yet" empty state, 5) Has "Add Book" button that navigates to AddBook route. Sort books alphabetically by title. Add #[cfg(test)] mod tests.</action>
  <behavior>
    - Test: LibraryScreen renders without books (empty state)
    - Test: LibraryScreen shows book list when books loaded
    - Test: Books sorted alphabetically by title
    - Test: "Add Book" button navigates to AddBook route
    - Test: Each book shows title and author
  </behavior>
  <verify>
    <automated>cargo test --lib ui::library::tests -- --nocapture</automated>
  </verify>
  <done>LibraryScreen component renders book list, loads books on mount, navigates to add book</done>
</task>

<task type="auto" tdd="true">
  <name>task 3: Create AddBookForm component with modal</name>
  <files>src/ui/add_book.rs</files>
  <action>Create new file src/ui/add_book.rs with AddBookForm component that: 1) Has controlled inputs for title and author (required), 2) Has optional "Add cover photo" button (placeholder for camera - Phase 2), 3) Validates title + author not empty, 4) Calls db.create_book() on submit, 5) Navigates to book detail on success (placeholder route). Use modal overlay styling. Add #[cfg(test)] mod tests.</action>
  <behavior>
    - Test: Form renders with title and author inputs
    - Test: Submit disabled when title empty
    - Test: Submit disabled when author empty
    - Test: Submit enabled when both fields filled
    - Test: Submit calls create_book with book data
    - Test: Success navigates to book detail route
  </behavior>
  <verify>
    <automated>cargo test --lib ui::add_book::tests -- --nocapture</automated>
  </verify>
  <done>AddBookForm component validates inputs, saves book, navigates on success</done>
</task>

<task type="auto">
  <name>task 4: Wire up router and navigation</name>
  <files>src/app.rs, src/ui/mod.rs</files>
  <action>Update src/app.rs Router component to include LibraryScreen and AddBookForm routes. Update src/ui/mod.rs to export new components. Ensure navigation from LibraryScreen "Add Book" button to AddBookForm works. Test full flow: app launch -> library -> add book -> back to library.</action>
  <verify>
    <automated>cargo check && cargo test --lib ui -- --nocapture</automated>
  </verify>
  <done>Router configured, navigation working between library and add book</done>
</task>

</tasks>

<verification>
- cargo check (compiles without errors)
- cargo test --lib ui::library::tests -- --nocapture (library tests pass)
- cargo test --lib ui::add_book::tests -- --nocapture (add book tests pass)
</verification>

<success_criteria>
- LibraryScreen displays book list or empty state
- AddBookForm validates and saves books
- Navigation between routes working
- All UI tests passing
- App compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-infrastructure/01-03-SUMMARY.md`
</output>
