---
phase: 01-core-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/core/storage.rs, src/core/db.rs]
autonomous: true
requirements: [CORE-03]

must_haves:
  truths:
    - "Cover photos are saved to filesystem"
    - "File paths are stored in database"
    - "Images can be retrieved from storage"
  artifacts:
    - path: "src/core/storage.rs"
      provides: "File storage operations for images"
      min_lines: 60
    - path: "src/core/db.rs"
      provides: "Book update with cover_path"
      exports: ["update_book"]
  key_links:
    - from: "src/core/storage.rs"
      to: "src/core/db.rs"
      via: "save_cover_photo calls update_book"
      pattern: "update_book.*cover_path"
---

<objective>
Implement filesystem storage for cover photos with database path references

Purpose: Enable image storage without SQLite BLOB memory issues
Output: Working file storage system integrated with book database
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-infrastructure/01-CONTEXT.md
@.planning/phases/01-core-infrastructure/01-RESEARCH.md

@src/core/db.rs
@src/platform/android.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: Create storage module with save/load operations</name>
  <files>src/core/storage.rs</files>
  <action>Create new file src/core/storage.rs with StorageService struct. Implement: new(assets_dir: PathBuf) -> Self, save_image(&self, data: &[u8], prefix: &str) -> Result<String> (saves file, returns relative path), get_image(&self, path: &str) -> Result<Vec<u8>>, delete_image(&self, path: &str) -> Result<()>. Store images in {assets_dir}/images/ subdirectory. Use UUID-based filenames. Add #[cfg(test)] mod tests.</action>
  <behavior>
    - Test: save_image writes file to correct directory
    - Test: save_image returns relative path (not absolute)
    - Test: get_image reads file content back
    - Test: delete_image removes file
    - Test: get_image returns error for non-existent file
    - Test: Images directory created if not exists
  </behavior>
  <verify>
    <automated>cargo test --lib storage::tests -- --nocapture</automated>
  </verify>
  <done>StorageService implemented with save/load/delete, all tests passing</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: Add cover photo integration to database</name>
  <files>src/core/db.rs</files>
  <action>Add to db.rs: save_cover_photo(&self, book_id: &str, image_data: &[u8], storage: &StorageService) -> Result<String> method that: 1) Calls storage.save_image() to save file, 2) Updates book.cover_path with returned path, 3) Returns the path. Also add remove_cover_photo(&self, book_id: &str, storage: &StorageService) -> Result<()> that deletes file and clears database field. Add integration tests.</action>
  <behavior>
    - Test: save_cover_photo saves file and updates database
    - Test: save_cover_photo returns stored path
    - Test: remove_cover_photo deletes file and clears database field
    - Test: get_book returns book with cover_path after save
  </behavior>
  <verify>
    <automated>cargo test --lib db::tests::cover_photo -- --nocapture</automated>
  </verify>
  <done>Cover photo save/remove methods working with database integration</done>
</task>

<task type="auto">
  <name>task 3: Add Android-specific storage path resolution</name>
  <files>src/platform/android.rs</files>
  <action>Extend android.rs with get_assets_directory() -> Result<PathBuf> function that uses JNI to call Context.getFilesDir() on Android activity. Fallback to std::env::current_dir() for non-Android platforms. Export function for use by storage module.</action>
  <verify>
    <automated>cargo test --lib platform::android::tests -- --nocapture</automated>
  </verify>
  <done>Android assets directory resolution working, fallback for desktop</done>
</task>

</tasks>

<verification>
- cargo test --lib storage::tests -- --nocapture (storage tests pass)
- cargo test --lib db::tests::cover-photo -- --nocapture (integration tests pass)
</verification>

<success_criteria>
- StorageService saves images to filesystem
- File paths stored in database (not BLOBs)
- Images retrievable from storage
- Android-specific path resolution working
- All storage tests passing
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-infrastructure/01-02-SUMMARY.md`
</output>
