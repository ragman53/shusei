---
phase: 01-core-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/core/models.rs, src/core/db.rs]
autonomous: true
requirements: [CORE-02, CORE-05]

must_haves:
  truths:
    - "Book metadata can be saved to database"
    - "Book data persists after app restart"
    - "No memory leaks from database operations"
  artifacts:
    - path: "src/core/models.rs"
      provides: "Book struct with serialization"
      min_lines: 40
    - path: "src/core/db.rs"
      provides: "Books table schema and CRUD operations"
      contains: "CREATE TABLE books"
  key_links:
    - from: "src/core/db.rs"
      to: "src/core/models.rs"
      via: "Book struct usage"
      pattern: "use crate::core::models::Book"
---

<objective>
Create database foundation with Book model and books table schema

Purpose: Establish the data layer that all book operations depend on
Output: Working database schema with tested CRUD operations
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-infrastructure/01-CONTEXT.md
@.planning/phases/01-core-infrastructure/01-RESEARCH.md

@src/core/db.rs
@src/core/error.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: Create Book model with serialization</name>
  <files>src/core/models.rs</files>
  <action>Create new file src/core/models.rs with Book struct containing: id (String UUID), title (String), author (String), cover_path (Option<String>), pages_captured (i32), total_pages (Option<i32>), last_opened_at (Option<i64>), created_at (i64). Implement Default trait, serde Serialize/Deserialize, and FromRow trait for rusqlite. Add #[cfg(test)] mod tests with serialization round-trip tests.</action>
  <behavior>
    - Test: Book serializes to JSON and deserializes back
    - Test: Book with minimal fields (title, author) creates successfully
    - Test: Book with all fields creates successfully
    - Test: Default trait provides sensible defaults
  </behavior>
  <verify>
    <automated>cargo test --lib models::tests -- --nocapture</automated>
  </verify>
  <done>Book struct defined with all fields, Serialize/Deserialize/FromRow traits implemented, tests passing</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: Add books table schema to db.rs</name>
  <files>src/core/db.rs</files>
  <action>Extend existing db.rs migrate() function to create books table with schema from RESEARCH.md: id TEXT PRIMARY KEY, title TEXT NOT NULL, author TEXT NOT NULL, cover_path TEXT, pages_captured INTEGER DEFAULT 0, total_pages INTEGER, last_opened_at INTEGER, created_at INTEGER NOT NULL. Add index on title. Enable WAL mode with PRAGMA journal_mode=WAL. Add #[cfg(test)] mod tests with schema verification tests.</action>
  <behavior>
    - Test: books table exists after migration
    - Test: idx_books_title index exists
    - Test: WAL mode is enabled
    - Test: Inserting valid book succeeds
    - Test: Inserting book without title fails
    - Test: Inserting book without author fails
  </behavior>
  <verify>
    <automated>cargo test --lib db::tests::books_schema -- --nocapture</automated>
  </verify>
  <done>Books table created with all columns, index, WAL mode enabled, schema tests passing</done>
</task>

<task type="auto" tdd="true">
  <name>task 3: Implement book CRUD operations</name>
  <files>src/core/db.rs</files>
  <action>Add to db.rs: create_book(&self, book: &Book) -> Result<String> (returns book id), get_book(&self, id: &str) -> Result<Option<Book>>, get_all_books(&self) -> Result<Vec<Book>>, update_book(&self, book: &Book) -> Result<()>, delete_book(&self, id: &str) -> Result<()>. Use prepared statements with parameters. Add #[cfg(test)] mod tests::books with CRUD operation tests.</action>
  <behavior>
    - Test: create_book inserts and returns generated id
    - Test: get_book retrieves by id
    - Test: get_all_books returns all books sorted by title
    - Test: update_book modifies existing book
    - Test: delete_book removes book
    - Test: get_book returns None for non-existent id
  </behavior>
  <verify>
    <automated>cargo test --lib db::tests::books_crud -- --nocapture</automated>
  </verify>
  <done>All 5 CRUD operations implemented, tested, and passing</done>
</task>

</tasks>

<verification>
- cargo test --lib db::tests -- --nocapture (all database tests pass)
- cargo test --lib models::tests -- --nocapture (all model tests pass)
</verification>

<success_criteria>
- Book model defined with all required fields
- Books table created with correct schema
- WAL mode enabled for concurrent reads
- All CRUD operations working
- Unit tests passing for models and database operations
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-infrastructure/01-01-SUMMARY.md`
</output>
