---
phase: 01-core-infrastructure
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03"]
files_modified: [src/platform/android.rs, src/core/state.rs, src/app.rs]
autonomous: false
requirements: [CORE-04, CORE-05]

user_setup:
  - service: android_emulator
    why: "Testing Android lifecycle requires actual Android environment"
    env_vars: []
    dashboard_config:
      - task: "Set up Android emulator or connect physical device"
        location: "Android Studio -> Device Manager"

must_haves:
  truths:
    - "App survives background/foreground transition"
    - "State restored after app killed and reopened"
    - "No JNI memory leaks during normal operation"
  artifacts:
    - path: "src/platform/android.rs"
      provides: "Lifecycle handlers (onPause, onResume)"
      min_lines: 100
    - path: "src/core/state.rs"
      provides: "AppState serialization and persistence"
      min_lines: 60
  key_links:
    - from: "src/platform/android.rs"
      to: "src/core/state.rs"
      via: "save_state/load_state calls"
      pattern: "save_state|load_state"
    - from: "src/platform/android.rs"
      to: "JNI environment"
      via: "PushLocalFrame/PopLocalFrame"
      pattern: "push_local_frame|pop_local_frame"
---

<objective>
Implement Android lifecycle handling with state persistence and JNI memory management

Purpose: Ensure app handles background transitions gracefully without memory leaks
Output: Lifecycle-aware app with state restoration and clean JNI patterns
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-infrastructure/01-CONTEXT.md
@.planning/phases/01-core-infrastructure/01-RESEARCH.md

@src/platform/android.rs
@src/app.rs
@src/core/db.rs
</context>

<interfaces>
<!-- Key types from existing code -->

From src/platform/android.rs (existing):
```rust
// JNI setup already exists, extend with lifecycle handlers
#[jni_fn]
pub fn on_pause(env: Env, activity: JObject) -> Result<()>;
#[jni_fn]
pub fn on_resume(env: Env, activity: JObject) -> Result<()>;
```

From src/app.rs (after Plan 03):
```rust
#[derive(Routable, Clone, PartialEq, Debug)]
enum Route { /* routes */ }
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: Create AppState struct with serialization</name>
  <files>src/core/state.rs</files>
  <action>Create new file src/core/state.rs with AppState struct containing: current_route (String), scroll_position (f32), timestamp (i64). Implement serde Serialize/Deserialize. Add save_to_prefs(&self) -> Result<()> and load_from_prefs() -> Result<Option<Self>> functions using key-value storage (use existing patterns or simple JSON file). Add #[cfg(test)] mod tests.</action>
  <behavior>
    - Test: AppState serializes to JSON
    - Test: AppState deserializes from JSON
    - Test: save_to_prefs writes to file
    - Test: load_from_prefs reads from file
    - Test: load_from_prefs returns None if file doesn't exist
  </behavior>
  <verify>
    <automated>cargo test --lib state::tests -- --nocapture</automated>
  </verify>
  <done>AppState struct with serialization and persistence working</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: Implement Android lifecycle handlers</name>
  <files>src/platform/android.rs</files>
  <action>Extend android.rs with: on_pause(env, activity) that saves AppState before background, on_resume(env, activity) that restores AppState on return. Use PushLocalFrame/PopLocalFrame for JNI operations. Call state.save_to_prefs() in on_pause, state.load_from_prefs() in on_resume. Add proper error handling. Add #[cfg(test)] mod tests with mock JNI environment.</action>
  <behavior>
    - Test: on_pause calls save_to_prefs
    - Test: on_resume calls load_from_prefs
    - Test: JNI local frames properly managed (no leaks)
    - Test: Errors handled gracefully (don't crash)
  </behavior>
  <verify>
    <automated>cargo test --lib platform::android::tests::lifecycle -- --nocapture</automated>
  </verify>
  <done>onPause/onResume handlers implemented with state save/restore</done>
</task>

<task type="auto">
  <name>task 3: Wire lifecycle into app initialization</name>
  <files>src/app.rs, src/main.rs</files>
  <action>Update app initialization to: 1) Load AppState on startup, 2) Restore last route if available, 3) Register lifecycle callbacks with Android system. Ensure state restoration works end-to-end. Document JNI cleanup patterns in code comments.</action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>App initializes with state restoration, lifecycle callbacks registered</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>task 4: Verify lifecycle handling on Android device</name>
  <files>src/platform/android.rs</files>
  <action>Manual verification of Android lifecycle handling. This checkpoint validates that state persistence and JNI memory management work correctly on actual Android hardware/emulator.</action>
  <what-built>Complete Android lifecycle handling with state persistence and JNI memory management</what-built>
  <how-to-verify>
    1. Build APK: `cargo ndk build --release`
    2. Install on Android emulator or device
    3. Launch app, navigate to library screen
    4. Press home button (background app)
    5. Wait 5 seconds
    6. Reopen app from recent apps
    7. Verify: Same screen restored, no crash
    8. Repeat 10 times to verify stability
    
    For JNI memory check (optional):
    1. Open Android Studio Profiler
    2. Attach to running app
    3. Monitor native memory
    4. Perform 20+ database operations
    5. Verify: No continuous memory growth
  </how-to-verify>
  <verify>
    <manual>cargo ndk build --release && install on device && test background/foreground 10x</manual>
  </verify>
  <done>App survives background/foreground transitions without crashes, state restored correctly</done>
  <resume-signal>Type "lifecycle verified" or describe issues found</resume-signal>
</task>
</task>

</tasks>

<verification>
- cargo test --lib state::tests -- --nocapture (state tests pass)
- cargo test --lib platform::android::tests::lifecycle -- --nocapture (lifecycle tests pass)
- cargo check (compiles without errors)
- Manual lifecycle testing on Android device/emulator (checkpoint)
</verification>

<success_criteria>
- AppState serializes and persists correctly
- on_pause saves state before background
- on_resume restores state on return
- JNI memory properly managed with PushLocalFrame/PopLocalFrame
- App survives background/foreground transitions
- Manual verification passed on Android device
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-infrastructure/01-04-SUMMARY.md`
</output>
