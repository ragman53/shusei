---
phase: 03-pdf-support
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [src/ui/reader.rs, src/ui/components.rs]
autonomous: true
requirements: [PDF-03]
must_haves:
  truths:
    - "User can read converted PDF content in reflow mode"
    - "User can adjust font size with slider (12px-32px)"
    - "User can jump to specific page number"
    - "Reading shows progress (page X of Y)"
  artifacts:
    - path: "src/ui/reader.rs"
      provides: "Reflow reading view with font controls"
      pattern: "font_size|slider|input range"
    - path: "src/ui/components.rs"
      provides: "PageJumpModal component"
      exports: ["PageJumpModal"]
  key_links:
    - from: "src/ui/reader.rs"
      to: "src/core/db.rs"
      via: "get_pages_by_book query"
      pattern: "db\\.get_pages_by_book"
    - from: "src/ui/reader.rs"
      to: "src/ui/components.rs"
      via: "PageJumpModal usage"
      pattern: "PageJumpModal"
---

<objective>
Build reflow reading UI with font size controls, page navigation, and progress display.

Purpose: Provide comfortable reading experience for converted PDF content with user-controlled typography and easy navigation.

Output: Reflow reader component, font size slider, page jump modal, progress indicator.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-pdf-support/03-CONTEXT.md

@src/ui/reader.rs
@src/ui/components.rs
@src/core/db.rs
</context>

<interfaces>
<!-- Key types and contracts from existing code -->

From src/core/db.rs:
```rust
pub struct BookPage {
    pub id: i64,
    pub book_id: String,
    pub page_number: i32,
    pub image_path: String,
    pub ocr_markdown: String,
    pub ocr_text_plain: String,
    pub confidence: Option<f32>,
    pub created_at: i64,
}

pub fn get_pages_by_book(&self, book_id: &str) -> Result<Vec<BookPage>>;
pub fn get_book(&self, id: &str) -> Result<Option<Book>>;
```

From src/ui/reader.rs (existing shell):
```rust
#[component]
pub fn ReaderPage() -> Element;
// Has placeholder import button and book list
```
</interfaces>

<tasks>

<task type="auto">
  <name>task 1: Create reflow reading view with continuous scroll</name>
  <files>src/ui/reader.rs</files>
  <action>
    Build reflow reader for converted PDF content:
    1. Create new component: ReaderBookView(book_id: String)
    2. On mount: load book via db.get_book(), load pages via db.get_pages_by_book()
    3. Render as continuous scroll: div with overflow-y-auto, max-w-2xl mx-auto
    4. For each page: render ocr_markdown as HTML (use markdown-to-html crate or simple parsing)
    5. Show page separator with page number between pages
    6. Handle empty state: "No pages converted yet" with Convert button
    7. Add back button to return to library
    
    Use dioxus::html::formatted_string for markdown rendering or simple text display. Follow Phase 1 component patterns.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>Reflow reader displays converted pages in continuous scroll with page numbers</done>
</task>

<task type="auto">
  <name>task 2: Add font size slider control (12px-32px)</name>
  <files>src/ui/reader.rs</files>
  <action>
    Add font size control to reader:
    1. Add signal: let mut font_size = use_signal(|| 18); (default 18px)
    2. Add slider UI: input type="range" min="12" max="32" value={font_size}
    3. Show current size: span showing "{font_size}px"
    4. Apply to content: style="font-size: {font_size}px" on content container
    5. Persist preference: save to localStorage or app settings (optional enhancement)
    6. Position in header: next to back button
    
    Use Tailwind styling. Slider should show visual feedback as user drags. Range 12-32px per CONTEXT.md.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>Font size slider works, adjusts text from 12px to 32px in real-time</done>
</task>

<task type="auto">
  <name>task 3: Add page jump modal and progress indicator</name>
  <files>src/ui/reader.rs, src/ui/components.rs</files>
  <action>
    Add navigation features:
    1. Create PageJumpModal component in components.rs:
       - Input field for page number
       - Validate: 1 <= page <= total_pages
       - On submit: scroll to that page's content
    2. Add "Go to page" button in reader header (book icon or # symbol)
    3. Add reading progress: "Page {current} of {total}" in header
    4. Track current visible page via intersection observer or scroll position
    5. Auto-update progress as user scrolls
    
    Use dioxus modal pattern from Phase 1. Progress updates on scroll using use_effect with scroll event.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>Page jump modal works, progress indicator shows current page while scrolling</done>
</task>

<task type="auto">
  <name>task 4: Wire reader to library with PDF badge</name>
  <files>src/ui/library.rs, src/ui/components.rs</files>
  <action>
    Complete library integration:
    1. Add PDF badge to BookCard: if book.is_pdf, show "📄 PDF" icon/badge
    2. Show conversion progress: "{converted_pages}/{total_pages} pages"
    3. On tap: if pages exist, navigate to ReaderBookView; if no pages, show "Start Conversion" button
    4. Add filter toggle: "All" / "PDFs only" / "Physical only" buttons above list
    5. Filter state: let mut filter = use_signal(|| Filter::All)
    6. Apply filter to books list before rendering
    
    Use existing Link component for navigation. Badge styling: small pill with icon.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>Library shows PDF badges, conversion progress, filter toggle, navigates to reader</done>
</task>

</tasks>

<verification>
- Reflow reader displays converted PDF pages in continuous scroll
- Font size slider adjusts text from 12px to 32px
- Page jump modal validates input and scrolls to page
- Progress indicator shows current reading position
- Library displays PDF badges and conversion progress
- Filter toggle shows All/PDFs/Physical subsets
</verification>

<success_criteria>
1. User can read converted PDF content with comfortable continuous scroll
2. Font size adjustable via slider (12-32px range)
3. User can tap page number to jump to specific page
4. Reading progress visible (page X of Y)
5. Library shows PDF books with badges and conversion progress
6. Filter toggle works to show PDFs only or physical books only
7. cargo check passes (no compilation errors)
</success_criteria>

<output>
After completion, create `.planning/phases/03-pdf-support/03-03-SUMMARY.md`
</output>
