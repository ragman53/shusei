---
phase: 03-pdf-support
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [src/core/pdf.rs, src/core/ocr/engine.rs, src/core/db.rs]
autonomous: true
requirements: [PDF-02, PDF-04]
must_haves:
  truths:
    - "User can start OCR conversion on imported PDF"
    - "Progress shows stage (Rendering → OCR → Saving) and page numbers"
    - "Processing resumes from last page after interruption"
    - "Large PDFs (100+ pages) process without crashing"
  artifacts:
    - path: "src/core/pdf.rs"
      provides: "Batch page rendering with progress callback"
      exports: ["PdfProcessor::render_pages_batch()"]
    - path: "src/core/ocr/engine.rs"
      provides: "Parallel OCR processing for multiple pages"
      pattern: "tokio::spawn|join_all"
    - path: "src/core/db.rs"
      provides: "Progress tracking and resume support"
      contains: "processing_progress table or field"
  key_links:
    - from: "src/core/pdf.rs"
      to: "src/core/ocr/engine.rs"
      via: "render then OCR pipeline"
      pattern: "ocr_engine\\.process"
    - from: "src/core/ocr/engine.rs"
      to: "src/core/db.rs"
      via: "save_page calls"
      pattern: "db\\.save_page"
---

<objective>
Implement batch OCR processing pipeline with progress feedback and resume support.

Purpose: Convert PDF pages to text via OCR efficiently with visible progress, parallel processing, and ability to resume after interruption.

Output: Batch processing service, progress tracking, parallel OCR execution.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-pdf-support/03-CONTEXT.md

@src/core/pdf.rs
@src/core/ocr/engine.rs
@src/core/db.rs
@src/core/storage.rs
</context>

<interfaces>
<!-- Key types and contracts from existing code -->

From src/core/ocr/engine.rs (Phase 2):
```rust
pub struct OcrResult {
    pub markdown: String,
    pub plain_text: String,
    pub confidence: Option<f32>,
    pub language: Option<String>,
}

pub trait OcrEngine: Send + Sync {
    async fn process_image(&self, image_data: &[u8]) -> Result<OcrResult>;
}

pub struct NdlocrEngine { /* ... */ }
impl OcrEngine for NdlocrEngine { /* ... */ }
```

From src/core/db.rs:
```rust
pub struct NewBookPage {
    pub book_id: String,
    pub page_number: i32,
    pub image_path: String,
    pub ocr_markdown: String,
    pub ocr_text_plain: String,
    pub confidence: Option<f32>,
}

pub fn save_page(&self, page: &NewBookPage) -> Result<i64>;
pub fn get_pages_by_book(&self, book_id: &str) -> Result<Vec<BookPage>>;
```

From src/core/storage.rs:
```rust
pub fn save_image(&self, image_data: &[u8], prefix: &str) -> Result<String>;
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: Add progress tracking schema and methods</name>
  <files>src/core/db.rs</files>
  <behavior>
    - Test: Can create processing_progress record for a book
    - Test: Can update last_processed_page and status
    - Test: Can query progress by book_id
    - Test: Status transitions: processing → completed → failed
  </behavior>
  <action>
    Add progress tracking to database:
    1. Create table processing_progress in initialize_schema:
       CREATE TABLE processing_progress (
         book_id TEXT PRIMARY KEY REFERENCES books(id),
         last_processed_page INTEGER DEFAULT 0,
         total_pages INTEGER,
         status TEXT DEFAULT 'pending', -- pending, processing, completed, failed
         updated_at INTEGER
       )
    2. Add methods: create_progress, update_progress, get_progress
    3. Add ProcessingProgress struct with from_row
    4. Write tests in db.rs mod processing_progress
    
    Use same timestamp pattern as books table (Unix epoch seconds).
  </action>
  <verify>
    <automated>cargo test db::processing_progress -- --nocapture</automated>
  </verify>
  <done>Progress tracking schema and CRUD methods implemented, tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: Implement batch page rendering with progress</name>
  <files>src/core/pdf.rs</files>
  <behavior>
    - Test: render_pages_batch renders 10 pages at a time
    - Test: Progress callback called after each batch
    - Test: Skips already-rendered pages (resume support)
    - Test: Returns Vec<(page_number, image_bytes)> for batch
  </behavior>
  <action>
    Add batch rendering to PdfProcessor:
    1. Add method: pub async fn render_pages_batch(&self, document: &PdfDocument, book_id: &str, db: &Database, batch_size: u32) -> Result<Vec<(u32, Vec<u8>)>>
    2. Query processing_progress to get last_processed_page
    3. Render batch_size pages starting from last + 1
    4. Save each rendered image via storage.save_image(image, &format!("pdf_pages/{book_id}"))
    5. Update progress after batch complete
    6. Return rendered pages for OCR processing
    
    Use tokio::spawn for async rendering. Default batch_size = 10 pages per CONTEXT.md.
  </action>
  <verify>
    <automated>cargo test pdf::batch_rendering -- --nocapture</automated>
  </verify>
  <done>Batch rendering with resume support implemented, tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>task 3: Implement parallel OCR processing pipeline</name>
  <files>src/core/ocr/engine.rs</files>
  <behavior>
    - Test: process_pages_parallel processes 2-3 pages concurrently
    - Test: Each page OCR result saved to database
    - Test: Progress callback called after each page completes
    - Test: Handles OCR failures gracefully (marks page as failed, continues)
  </behavior>
  <action>
    Add parallel OCR to NdlocrEngine:
    1. Add method: pub async fn process_pages_parallel(&self, pages: Vec<(u32, Vec<u8>)>, book_id: &str, db: &Database, progress_cb: impl Fn(u32, u32)) -> Result<()>
    2. Use futures::future::join_all with concurrency limit (Semaphore with 3 permits)
    3. For each page: render → OCR → save_page to database
    4. Auto-retry on low confidence (retry 3 times then skip per CONTEXT.md)
    5. Skip quality warnings for PDFs (assume good quality)
    6. Call progress_cb(page_num, total) after each completes
    
    Use tokio::sync::Semaphore for concurrency control. Follow Phase 2 OCR patterns.
  </action>
  <verify>
    <automated>cargo test ocr::parallel_processing -- --nocapture</automated>
  </verify>
  <done>Parallel OCR pipeline with retry logic, tests pass</done>
</task>

<task type="auto">
  <name>task 4: Create PDF conversion service orchestrator</name>
  <files>src/core/pdf.rs</files>
  <action>
    Create high-level conversion service:
    1. Add struct PdfConversionService with fields: pdf_processor, ocr_engine, db, storage
    2. Add method: pub async fn convert_pdf(&self, book_id: &str, progress_cb: impl Fn(ConversionProgress)) -> Result<()>
    3. ConversionProgress struct: { stage: Stage, current_page: u32, total_pages: u32 }
    4. Stage enum: Rendering, OcrProcessing, Complete
    5. Loop: render_batch → process_parallel → update_progress until all pages done
    6. Update status to 'completed' when done, 'failed' on error
    
    Stage-based progress per CONTEXT.md: "Rendering pages → Running OCR → Saving..."
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>Conversion service orchestrates batch rendering + parallel OCR with stage progress</done>
</task>

</tasks>

<verification>
- Batch processing works: renders 10 pages, processes via OCR, saves to database
- Progress tracking persists: can resume after app restart
- Parallel OCR processes 2-3 pages concurrently
- Large PDFs handled: no memory spikes, processes in batches
- Stage-based progress reported correctly
</verification>

<success_criteria>
1. User can tap "Convert" on imported PDF and see stage-based progress
2. Processing runs in background, persists progress, can resume after interruption
3. OCR processes pages in parallel (2-3 concurrent)
4. Failed pages retry 3 times then skip, continue processing remaining pages
5. All tests pass (cargo test)
</success_criteria>

<output>
After completion, create `.planning/phases/03-pdf-support/03-02-SUMMARY.md`
</output>
