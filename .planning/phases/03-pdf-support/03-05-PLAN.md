---
phase: 03-pdf-support
plan: 05
type: execute
wave: 2
depends_on: ["03-04"]
files_modified: [src/core/ocr/engine.rs, src/core/ocr/mod.rs, Cargo.toml]
autonomous: true
requirements: [PDF-02]
gap_closure: true

must_haves:
  truths:
    - "OCR extracts actual text from PDF page images"
    - "Extracted text saved to database with markdown formatting"
    - "Reader displays real OCR content, not empty placeholders"
  artifacts:
    - path: "src/core/ocr/engine.rs"
      provides: "Working OCR with ONNX model loading and inference"
      exports: ["NdlocrEngine::process_image()"]
      min_lines: 200
    - path: "Cargo.toml"
      provides: "ONNX runtime dependencies"
      contains: "ort|tract-onnx"
  key_links:
    - from: "src/core/ocr/engine.rs"
      to: "ONNX models"
      via: "model loading"
      pattern: "ort::Session|tract_onnx"
    - from: "src/core/ocr/engine.rs"
      to: "src/core/db.rs"
      via: "save_page with OCR results"
      pattern: "db\\.save_page.*ocr_markdown"
---

<objective>
Complete OCR pipeline implementation with ONNX model loading and text extraction inference.

Purpose: Enable actual text extraction from PDF page images instead of returning empty results.

Output: Working OCR engine that loads NDLOCR-Lite models and extracts text from images with markdown formatting.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases\03-pdf-support\03-CONTEXT.md
@.planning/phases\03-pdf-support\03-VERIFICATION.md

@src/core/ocr/engine.rs
@src/core/ocr/mod.rs
@Cargo.toml
</context>

<interfaces>
<!-- Key types and contracts from existing code -->

From src/core/ocr/engine.rs (Plan 02):
```rust
pub struct OcrResult {
    pub markdown: String,
    pub plain_text: String,
    pub confidence: Option<f32>,
    pub language: Option<String>,
}

pub trait OcrEngine: Send + Sync {
    async fn process_image(&self, image_data: &[u8]) -> Result<OcrResult>;
}

pub struct NdlocrEngine {
    // TODO: Add ONNX session fields here
}

impl OcrEngine for NdlocrEngine {
    async fn process_image(&self, image_data: &[u8]) -> Result<OcrResult> {
        // CURRENT: Returns empty results (lines 143-150)
        // TODO: Implement full OCR pipeline
    }
}
```

From research/SUMMARY.md (Phase 2 research):
```
NDLOCR-Lite: Lightweight OCR model optimized for mobile
- Supports Japanese and English
- Outputs markdown with structure (headings, lists)
- Model files: ndlocr-lite.onnx (detection + recognition)
- Runtime: ort (ONNX Runtime) or tract-onnx (pure Rust)
```
</interfaces>

<tasks>

<task type="auto">
  <name>task 1: Add ONNX runtime dependency and model loading</name>
  <files>src/core/ocr/engine.rs, Cargo.toml</files>
  <action>
    Set up ONNX runtime infrastructure:
    
    1. Add dependencies to Cargo.toml:
       - ort = "2.0" (ONNX Runtime bindings)
       - image = "0.25" (image preprocessing)
       - ndarray = "0.15" (tensor operations)
    
    2. Update NdlocrEngine struct:
       - Add session: ort::Session field
       - Add model_path: PathBuf field
       - Add language: String field (ja/en)
    
    3. Implement NdlocrEngine::new(model_path: &Path, language: &str):
       - Load ONNX model: ort::Session::builder()?.commit_from_file(model_path)?
       - Validate model inputs/outputs
       - Store session and language
       - Return Result<NdlocrEngine>
    
    4. Add model download/bundling (choose one):
       Option A: Bundle model in app (add to assets/)
       Option B: Download on first run (add download logic)
       Option C: Require user to provide model path
    
    5. Write test: test_engine_creation (skip if model not present)
    
    Use ort crate (ONNX Runtime) as researched in Phase 2. Follow pattern from Phase 2 STT engine.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>NdlocrEngine loads ONNX model successfully, session created</done>
</task>

<task type="auto">
  <name>task 2: Implement image preprocessing pipeline</name>
  <files>src/core/ocr/engine.rs</files>
  <action>
    Add image preprocessing before OCR inference:
    
    1. Add preprocess_image(image_data: &[u8]) -> Result<ndarray::Array4<f32>>:
       - Decode image bytes using image::load_from_memory()
       - Convert to grayscale (if not already)
       - Resize to model input size (e.g., 960x960 for NDLOCR)
       - Normalize pixel values to 0.0-1.0 range
       - Convert to tensor (NCHW format: [1, 1, H, W])
       - Return tensor
    
    2. Add postprocess_output(output: ort::Value) -> Result<(Vec<String>, Vec<f32>)>:
       - Extract detection boxes from output[0]
       - Extract recognition text from output[1]
       - Extract confidence scores from output[2]
       - Return (text_lines, confidences)
    
    3. Handle language auto-detection (per CONTEXT.md):
       - Run language detection first (separate small model or heuristic)
       - Or use multilingual model that handles both ja/en
       - Store detected language in OcrResult.language
    
    4. Write test: test_preprocessing (uses sample image)
    
    Follow Phase 2 image preprocessing patterns (histogram equalization, downscaling).
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>Image preprocessing converts bytes to normalized tensor for model input</done>
</task>

<task type="auto">
  <name>task 3: Implement full OCR inference pipeline</name>
  <files>src/core/ocr/engine.rs</files>
  <action>
    Complete process_image() with full OCR pipeline:
    
    1. Replace empty return in process_image() with:
       - Preprocess: let tensor = preprocess_image(image_data)?
       - Create session inputs: vec![tensor.into_dyn().into()]
       - Run inference: let outputs = session.run(inputs)?
       - Postprocess: let (lines, confidences) = postprocess_output(outputs[0].clone())?
       - Calculate average confidence: confidences.iter().sum::<f32>() / confidences.len()
    
    2. Format output as markdown:
       - Join lines with newlines
       - Add markdown structure (headings, lists) if detected
       - Store in OcrResult.markdown
       - Also store plain text in OcrResult.plain_text
    
    3. Handle low confidence (auto-retry per CONTEXT.md):
       - If avg_confidence < 0.7: retry up to 3 times
       - Use different preprocessing (contrast enhancement)
       - If still low after 3 retries: return results with warning
    
    4. Skip quality warnings for PDFs (per CONTEXT.md)
    
    5. Write integration test: test_ocr_extraction (skip without model)
    
    Use markdown formatting to preserve structure. Handle both Japanese and English text.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>OCR extracts actual text from images, returns markdown with confidence scores</done>
</task>

<task type="auto">
  <name>task 4: Test OCR end-to-end with real PDF pages</name>
  <files>src/core/ocr/engine.rs</files>
  <action>
    Validate OCR pipeline with real-world testing:
    
    1. Create test PDF pages:
       - Use sample images from Phase 2 testing
       - Or render test PDF pages using pdfium
    
    2. Test full pipeline:
       - Load test image
       - Call ocr_engine.process_image(image_bytes).await
       - Verify: result.markdown is not empty
       - Verify: result.plain_text contains expected text
       - Verify: result.confidence is Some(value > 0.5)
       - Verify: result.language is detected (ja or en)
    
    3. Test edge cases:
       - Blurry images (should still extract some text)
       - Mixed language (Japanese + English)
       - Low contrast images
       - Different font sizes
    
    4. Performance test:
       - Measure inference time per page
       - Target: < 2 seconds per page on mid-range device
       - Log timing for monitoring
    
    5. Document model requirements:
       - Add README.md in ocr/ directory
       - Document how to download/bundle models
       - Document supported languages
    
    6. Update VERIFICATION.md with test results
    
    Test with both Japanese and English content. Target 95%+ accuracy on clear text.
  </action>
  <verify>
    <automated>cargo test ocr::tests -- --nocapture</automated>
  </verify>
  <done>OCR pipeline extracts text from real PDF pages with high accuracy</done>
</task>

</tasks>

<verification>
- ONNX model loads successfully without errors
- Image preprocessing produces valid tensor input
- OCR inference extracts actual text (not empty strings)
- Markdown formatting preserves document structure
- Confidence scores reflect extraction quality
- Auto-retry works on low confidence pages
- Performance meets target (< 2 sec/page)
</verification>

<success_criteria>
1. NdlocrEngine loads ONNX model and creates session
2. process_image() returns non-empty markdown and plain_text
3. Extracted text matches source image content (manual verification)
4. Confidence scores are accurate indicators of quality
5. OCR processes both Japanese and English text
6. All OCR tests pass (cargo test ocr::tests)
7. cargo check passes with no compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-pdf-support/03-05-SUMMARY.md`
</output>
