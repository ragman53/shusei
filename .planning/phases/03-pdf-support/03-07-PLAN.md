---
phase: 03-pdf-support
plan: 07
type: execute
wave: 2
depends_on: ["03-06"]
files_modified: [.planning/phases/03-pdf-support/03-VERIFICATION.md]
autonomous: false
requirements: [PDF-04]
gap_closure: true

must_haves:
  truths:
    - "Large PDFs (100+ pages) process without crashing"
    - "Memory usage remains stable during batch processing"
    - "User can continue using app while background processing occurs"
  artifacts:
    - path: ".planning/phases/03-pdf-support/03-VERIFICATION.md"
      provides: "Documented test results with large PDF"
      contains: "human_verification results"
  key_links:
    - from: "src/core/pdf.rs"
      to: "src/core/ocr/engine.rs"
      via: "batch processing pipeline"
      pattern: "render_pages_batch.*buffer_unordered"
---

<objective>
Verify large PDF processing (100+ pages) works without memory crashes on target devices.

Purpose: Confirm batch processing logic handles large files safely on low-RAM devices.

Output: Documented test results confirming stability with large PDFs.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-pdf-support/03-CONTEXT.md
@.planning/phases/03-pdf-support/03-VERIFICATION.md

@src/core/pdf.rs
@src/core/ocr/engine.rs
</context>

<interfaces>
<!-- Key batch processing logic from existing code -->

From src/core/pdf.rs (lines 331-357):
```rust
pub async fn render_pages_batch(
    &self,
    book_id: i64,
    batch_size: usize,  // 10 pages per batch
) -> Result<()> {
    // Batch processing implemented
    // Processes 10 pages at a time
    // Saves progress to processing_progress table
}
```

From src/core/ocr/engine.rs (line 359):
```rust
// Parallel OCR with concurrency limit
let results = futures_stream::iter(page_images)
    .map(|img| self.process_image(img))
    .buffer_unordered(3)  // Max 3 concurrent OCR operations
    .collect::<Vec<_>>()
    .await;
```

From VERIFICATION.md (lines 33-40):
```
Gap: "Large PDFs (100+ pages) process without crashing"
Status: partial
Reason: Batch processing logic exists but needs human testing with real large PDFs
Missing: Human testing with 100+ page PDF
```
</interfaces>

<tasks>

<task type="auto">
  <name>task 1: Prepare large PDF test file</name>
  <files>tests/large_pdf_test.pdf (or document source)</files>
  <action>
    Set up test infrastructure for large PDF testing:
    
    1. Obtain or create a 100+ page PDF:
       Option A: Use existing large PDF from test corpus
       - Find a public domain book with 100+ pages (e.g., Project Gutenberg)
       - Download PDF version
       - Document source and page count
       
       Option B: Create synthetic large PDF:
       - Combine multiple smaller PDFs
       - Or generate PDF with 100+ pages of test content
       - Ensure variety in page content (text density, images, etc.)
       
       Option C: Use existing app test PDF:
       - If app already has test PDFs, use the largest one
       - Verify it has 100+ pages
       
       Target: 100-200 pages (realistic large book, not extreme)
    
    2. Document test PDF metadata:
       - Page count
       - File size (MB)
       - Average page complexity (text-heavy, image-heavy, mixed)
       - Language content (Japanese, English, mixed)
    
    3. Set up monitoring infrastructure:
       - Add logging to render_pages_batch() for memory usage
       - Add logging to process_image() for inference time
       - Log batch completion times
       - Track total processing time
    
    4. Create test script (optional but helpful):
       - Script to import PDF, trigger conversion, monitor progress
       - Log output to file for analysis
       - Measure peak memory usage
    
    5. Document test procedure:
       - Step-by-step instructions for human tester
       - Expected behavior at each stage
       - Success criteria (no crash, stable memory, completes)
    
    Use Project Gutenberg or similar for public domain test content. Target realistic book sizes (150-200 pages typical for textbooks).
  </action>
  <verify>
    <automated>test -f tests/large_pdf_test.pdf || echo "TEST_PDF_NOT_FOUND - document source in summary"</automated>
  </verify>
  <done>Large PDF (100+ pages) ready for testing, test procedure documented</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>task 2: Test large PDF processing end-to-end</name>
  <files>tests/large_pdf_test.pdf, .planning/phases/03-pdf-support/03-VERIFICATION.md</files>
  <action>
    Manual verification of large PDF processing stability on actual Android device.
    This checkpoint validates that batch processing handles 100+ page PDFs without
    memory crashes, and resume functionality works correctly after backgrounding.
  </action>
  <what-built>
    Batch processing infrastructure for large PDFs:
    
    Existing implementation (from Plan 02 + Plan 05):
    - src/core/pdf.rs: render_pages_batch() processes 10 pages at a time
    - src/core/ocr/engine.rs: buffer_unordered(3) limits concurrent OCR to 3
    - processing_progress table tracks resume point
    - Progress display shows stage and page numbers
    
    What to test:
    - Import 100+ page PDF
    - Trigger full conversion
    - Monitor for crashes, memory issues, completion
  </what-built>
  <how-to-verify>
    1. Build and launch the app:
       cargo build --release --features pdf
       (Deploy to Android device or run on emulator)
    
    2. Import the large PDF:
       - Open app
       - Click "+" button to add book
       - Select "Import PDF"
       - Choose the 100+ page test PDF
       - Verify: Metadata dialog appears with correct page count
    
    3. Start conversion:
       - Go to book detail page
       - Click "Convert" button
       - Verify: Progress display appears showing "Rendering pages → Running OCR → Saving..."
       - Verify: Progress shows current page number (e.g., "Processing page 15/150")
    
    4. Monitor processing:
       - Watch for crash indicators:
         * App freezes completely (not just busy indicator)
         * App closes unexpectedly
         * "Out of memory" errors
         * ANR (Application Not Responding) dialogs
       - Monitor memory usage (if possible):
         * Android Studio Profiler
         * Or device settings → Developer options → Memory
       - Note processing speed:
         * Time per batch (10 pages)
         * Total time for full PDF
         * Any slowdowns or stalls
    
    5. Test multitasking during processing:
       - While processing: Press home button to background app
       - Use other apps for 1-2 minutes
       - Return to Shusei app
       - Verify: Processing resumed from last page (not restarted)
       - Verify: No crash on resume
    
    6. Verify completion:
       - Wait for full conversion to complete
       - Verify: Progress shows "150/150 pages converted" or similar
       - Open reader view
       - Verify: All pages display OCR text (spot check random pages)
       - Verify: No missing pages or empty content
    
    7. Document results:
       - Total processing time: ___ minutes
       - Peak memory usage: ___ MB (if measurable)
       - Any crashes or errors: Yes/No (describe)
       - Resume after backgrounding: Worked/Failed
       - Final page count converted: ___ / ___
    
    Expected outcome:
    - Processing completes without crash
    - Memory usage stays stable (no continuous growth)
    - App remains responsive during processing
    - Resume works correctly after backgrounding
    - All pages converted successfully
  </how-to-verify>
  <verify>
    <automated>test -f tests/large_pdf_test.pdf && echo "TEST_PDF_READY" || echo "TEST_PDF_MISSING"</automated>
  </verify>
  <done>Human confirms 100+ page PDF processes without crash, resume works</done>
  <resume-signal>Type "verified" if 100+ page PDF processes without crash, or describe issues found</resume-signal>
</task>

<task type="auto">
  <name>task 3: Document test results and update VERIFICATION.md</name>
  <files>.planning/phases/03-pdf-support/03-VERIFICATION.md</files>
  <action>
    Update verification status based on test results:
    
    1. Add test results to VERIFICATION.md:
       - Update re_verification section:
         * Add "Large PDF processing" to gaps_closed (if passed)
         * Or keep in gaps_remaining with details (if failed)
       - Update Observable Truths table:
         * Truth #7: "Large PDFs (100+ pages) process without crashing"
         * Status: ✓ VERIFIED or ⚠️ PARTIAL or ✗ FAILED
         * Evidence: Document test results
    
    2. Document test configuration:
       - Device model and specs (RAM, CPU)
       - Android version
       - App version/build number
       - Test PDF details (page count, file size, source)
    
    3. Record performance metrics:
       - Total processing time
       - Average time per page
       - Peak memory usage (if measured)
       - Any performance issues observed
    
    4. Note any issues or limitations:
       - Memory warnings on low-RAM devices
       - Slowdown after N pages
       - Specific page types that cause issues
       - Recommended maximum PDF size
    
    5. Update Requirements Coverage:
       - PDF-04: "Progress display for PDF processing"
       - Status: ✓ SATISFIED (if test passes)
       - Evidence: Large PDF test results
    
    6. If test revealed issues:
       - Create new gap entry with details
       - Suggest fixes (e.g., reduce batch_size, lower concurrency)
       - Link to relevant code locations
    
    7. Add to Human Verification section:
       - Document test procedure used
       - Include screenshots if helpful
       - Note any manual steps required
    
    Use format consistent with existing VERIFICATION.md entries. Be specific about test conditions and results.
  </action>
  <verify>
    <automated>grep -q "Large PDFs.*✓ VERIFIED" .planning/phases/03-pdf-support/03-VERIFICATION.md && echo "GAP_CLOSED=true" || echo "GAP_DOCUMENTED=true"</automated>
  </verify>
  <done>VERIFICATION.md updated with test results, gap status documented</done>
</task>

</tasks>

<verification>
- Large PDF test file obtained or created (100+ pages)
- Test procedure documented and executed
- Processing completes without crash on target device
- Memory usage remains stable (no OOM errors)
- Resume after backgrounding works correctly
- All pages converted successfully
- VERIFICATION.md updated with test results
- Gap status changed from "partial" to "verified" or documented issues
</verification>

<success_criteria>
1. Test PDF: 100+ page PDF available for testing
2. No crashes: Processing completes without app crash or OOM error
3. Stable memory: No continuous memory growth during processing
4. Resume works: Backgrounding and returning resumes from last page
5. Full conversion: All pages converted successfully
6. Documented: VERIFICATION.md updated with test results and metrics
7. Human verification: Tester confirms stability with large PDF
</success_criteria>

<output>
After completion, create `.planning/phases/03-pdf-support/03-07-SUMMARY.md`
</output>
