---
phase: 03-pdf-support
plan: 06
type: execute
wave: 1
depends_on: []
files_modified: [src/core/ocr/engine.rs, src/core/ocr/mod.rs, assets/ocr/models/]
autonomous: false
requirements: [PDF-02]
gap_closure: true

must_haves:
  truths:
    - "OCR extracts actual text from PDF page images"
    - "Extracted text saved to database with markdown formatting"
    - "Reader displays real OCR content, not empty placeholders"
  artifacts:
    - path: "src/core/ocr/engine.rs"
      provides: "Working OCR with ONNX model loading and inference"
      exports: ["NdlocrEngine::process_image()"]
      min_lines: 250
    - path: "assets/ocr/models/"
      provides: "Bundled NDLOCR-Lite ONNX models"
      contains: "text_detection.onnx, text_recognition.onnx"
  key_links:
    - from: "src/core/ocr/engine.rs"
      to: "assets/ocr/models/"
      via: "model loading from assets"
      pattern: "Session::builder.*commit_from_file"
    - from: "src/core/ocr/engine.rs"
      to: "src/core/db.rs"
      via: "save_page with OCR results"
      pattern: "db\\.save_page.*ocr_markdown"
---

<objective>
Complete OCR pipeline by bundling ONNX models and implementing postprocessing logic to extract actual text from model outputs.

Purpose: Fix the blocker preventing OCR from returning real text (currently returns empty vectors).

Output: Working OCR engine that loads bundled NDLOCR-Lite models and extracts text with markdown formatting.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-pdf-support/03-CONTEXT.md
@.planning/phases/03-pdf-support/03-VERIFICATION.md
@.planning/phases/03-pdf-support/03-05-PLAN.md

@src/core/ocr/engine.rs
@src/core/ocr/mod.rs
@Cargo.toml
</context>

<interfaces>
<!-- Key types and contracts from existing code -->

From src/core/ocr/engine.rs (Plan 02 + Plan 05):
```rust
pub struct OcrResult {
    pub markdown: String,
    pub plain_text: String,
    pub confidence: Option<f32>,
    pub language: Option<String>,
}

pub trait OcrEngine: Send + Sync {
    async fn process_image(&self, image_data: &[u8]) -> Result<OcrResult>;
}

pub struct NdlocrEngine {
    session: ort::Session,
    model_path: PathBuf,
    language: String,
}

// CURRENT ISSUE (VERIFICATION.md lines 24-30):
// - Lines 196-207: postprocess_onnx_output() returns empty vectors
// - Lines 279-295: run_inference_and_extract() returns empty (Vec::new(), Vec::new())
// - ONNX models not bundled
```

From Phase 2 research (NDLOCR-Lite):
```
NDLOCR-Lite outputs:
- Detection: Bounding boxes [y1, x1, y2, x2, confidence]
- Recognition: Text sequences (character IDs or direct text)
- Confidence: Per-line confidence scores

Model files needed:
- text_detection.onnx (detects text regions)
- text_recognition.onnx (recognizes text in regions)
```
</interfaces>

<tasks>

<task type="auto">
  <name>task 1: Bundle NDLOCR-Lite ONNX models in assets</name>
  <files>assets/ocr/models/, src/core/ocr/mod.rs</files>
  <action>
    Set up model bundling infrastructure:
    
    1. Create assets directory structure:
       - mkdir -p assets/ocr/models/
       - This directory will contain bundled ONNX models
    
    2. Download NDLOCR-Lite models (use one of these approaches):
       Option A: Download from Hugging Face (preferred):
       - URL: https://huggingface.co/ndlocr-lite/models/main/text_detection.onnx
       - URL: https://huggingface.co/ndlocr-lite/models/main/text_recognition.onnx
       - Save to assets/ocr/models/
       
       Option B: Use build script to download on build:
       - Create build.rs that downloads models if not present
       - Check hash to verify integrity
       
       Option C: Include as git LFS or submodule:
       - Add models to repo via git LFS
       - Or create separate models repo as submodule
    
    3. Update src/core/ocr/mod.rs:
       - Add const MODEL_DETECTION_PATH: &str = "assets/ocr/models/text_detection.onnx"
       - Add const MODEL_RECOGNITION_PATH: &str = "assets/ocr/models/text_recognition.onnx"
       - Add function get_model_path(model_name: &str) -> PathBuf
       - Use include_bytes! or copy-to-target in build.rs
    
    4. Document model requirements:
       - Add README.md in assets/ocr/models/
       - Document model source, license, download instructions
       - Note model sizes (expect ~5-10MB each for lite models)
    
    5. Update .gitignore if needed:
       - Ensure .onnx files are NOT ignored (or use LFS)
       - Or document manual download step for developers
    
    Use Hugging Face as primary source (reliable, versioned). Include build.rs fallback for CI/CD.
  </action>
  <verify>
    <automated>test -f assets/ocr/models/text_detection.onnx && test -f assets/ocr/models/text_recognition.onnx && echo "MODELS_PRESENT=true"</automated>
  </verify>
  <done>ONNX models bundled in assets/ocr/models/, paths exported in mod.rs</done>
</task>

<task type="auto">
  <name>task 2: Implement postprocessing to parse ONNX model outputs</name>
  <files>src/core/ocr/engine.rs</files>
  <action>
    Fix the empty return issue in postprocessing (VERIFICATION.md lines 24-30):
    
    1. Fix postprocess_onnx_output() (lines 196-207):
       CURRENT: Returns (Vec::new(), Vec::new())
       
       IMPLEMENT:
       - Extract detection output tensor: let detections = output[0].try_extract_tensor::<f32>()?
       - Parse bounding boxes: iterate over detection tensor, filter by confidence threshold (e.g., > 0.5)
       - For each detection box: extract ROI from input image
       - Run recognition model on each ROI
       - Extract recognition output: let text_output = recognition_session.run([roi_tensor])?
       - Decode character IDs to text (use NDLOCR character mapping)
       - Collect (text_line, confidence) pairs
       - Return (Vec<String>, Vec<f32>)
    
    2. Fix run_inference_and_extract() (lines 279-295):
       CURRENT: Returns (Vec::new(), Vec::new())
       
       IMPLEMENT:
       - Call detection session: let det_output = detection_session.run([preprocessed_image])?
       - Call postprocess_onnx_output(det_output) to get (lines, confidences)
       - Format lines as markdown: join with newlines, add structure if detected
       - Calculate average confidence: confidences.iter().sum() / len
       - Detect language: use language field or auto-detect from character set
       - Return OcrResult { markdown, plain_text, confidence, language }
    
    3. Handle NDLOCR output format:
       Detection output shape: [1, num_detections, 5] where 5 = [x1, y1, x2, y2, confidence]
       Recognition output shape: [1, num_chars, char_vocab_size] or [1, seq_len, num_classes]
       
       Decode recognition:
       - Argmax over char_vocab_size to get character IDs
       - Map IDs to characters using NDLOCR character set
       - Handle CTC decoding if needed (remove duplicates, handle blank tokens)
    
    4. Add error handling:
       - If detection fails: return error with "No text detected"
       - If recognition fails: return partial results with warning
       - Log failures for debugging
    
    5. Write unit test: test_postprocessing (uses mock ONNX outputs)
    
    Use ort tensor extraction API. Follow NDLOCR documentation for output format parsing.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>postprocess_onnx_output() returns actual text lines and confidence scores</done>
</task>

<task type="auto">
  <name>task 3: Integrate model loading into NdlocrEngine initialization</name>
  <files>src/core/ocr/engine.rs</files>
  <action>
    Update NdlocrEngine::new() to load bundled models:
    
    1. Update NdlocrEngine::new() signature:
       pub fn new(language: &str) -> Result<Self>
       (Remove model_path parameter, use bundled models)
    
    2. Load detection model:
       - let model_path = get_model_path("text_detection")
       - let detection_session = ort::Session::builder()?.commit_from_file(model_path)?
       - Store in NdlocrEngine.detection_session
    
    3. Load recognition model:
       - let model_path = get_model_path("text_recognition")
       - let recognition_session = ort::Session::builder()?.commit_from_file(model_path)?
       - Store in NdlocrEngine.recognition_session
    
    4. Validate model inputs:
       - Check detection input shape: [1, 3, H, W] or [1, 1, H, W]
       - Check recognition input shape: [1, 3, 48, W] (typical for text recognition)
       - Log model metadata (input/output shapes, opset version)
    
    5. Handle model loading errors:
       - If model not found: return error with download instructions
       - If model corrupt: return error with hash mismatch details
       - If ort version incompatible: return error with version requirements
    
    6. Update NdlocrEngine struct:
       - Replace session: ort::Session with:
         - detection_session: ort::Session
         - recognition_session: ort::Session
       - Keep language: String field
    
    7. Write test: test_engine_creation (verifies models load)
    
    Use bundled model paths from mod.rs. Follow Phase 2 STT engine loading pattern.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>NdlocrEngine initializes with bundled ONNX models, sessions created</done>
</task>

<task type="auto">
  <name>task 4: Test OCR end-to-end with real PDF pages</name>
  <files>src/core/ocr/engine.rs</files>
  <action>
    Validate OCR pipeline extracts actual text:
    
    1. Create integration test: test_ocr_extraction:
       - Load sample PDF page image (use Phase 2 test images)
       - Or render a test PDF page using pdfium
       - Call engine.process_image(image_bytes).await
       - Assert: result.markdown is not empty
       - Assert: result.plain_text contains expected keywords
       - Assert: result.confidence is Some(value > 0.5)
       - Assert: result.language is detected (ja or en)
    
    2. Test with known text:
       - Create test image with known text (e.g., "Hello World" or "こんにちは")
       - Run OCR
       - Verify extracted text matches (allowing for minor OCR errors)
       - Target: 90%+ character accuracy on clear text
    
    3. Test edge cases:
       - Blurry images (should still extract some text)
       - Mixed language (Japanese + English on same page)
       - Low contrast images
       - Different font sizes (12px, 18px, 24px)
       - Handwritten-style fonts (if supported)
    
    4. Performance benchmark:
       - Measure inference time per page
       - Target: < 2 seconds per page on mid-range device
       - Log: detection_time, recognition_time, total_time
       - Test with different image sizes (A4, A5, letter)
    
    5. Test auto-retry logic (per CONTEXT.md):
       - Simulate low confidence (< 0.7)
       - Verify retry happens (up to 3 times)
       - Verify different preprocessing on retry (contrast enhancement)
    
    6. Update VERIFICATION.md:
       - Document test results
       - Note any limitations or known issues
       - Mark gap as closed if tests pass
    
    7. Document usage:
       - Add example code to ocr/README.md
       - Document supported languages
       - Document accuracy expectations
    
    Test with both Japanese and English content. Include sample test images in tests/ directory.
  </action>
  <verify>
    <automated>cargo test ocr::tests::test_ocr_extraction -- --nocapture</automated>
  </verify>
  <done>OCR pipeline extracts text from real PDF pages with 90%+ accuracy</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>task 5: Verify OCR extracts real text from actual PDF</name>
  <files>src/core/ocr/engine.rs, assets/ocr/models/</files>
  <action>
    Manual verification of OCR text extraction on actual device/emulator.
    This checkpoint validates that bundled ONNX models load correctly and
    extract real text from PDF pages in the production app environment.
  </action>
  <what-built>
    Complete OCR pipeline with bundled ONNX models and working postprocessing.
    
    Files modified:
    - src/core/ocr/engine.rs: Model loading, postprocessing, inference
    - src/core/ocr/mod.rs: Model path exports
    - assets/ocr/models/: Bundled ONNX models
    
    Expected behavior:
    - NdlocrEngine loads models from assets/ocr/models/
    - process_image() returns non-empty markdown and plain_text
    - Extracted text matches source PDF content
  </what-built>
  <how-to-verify>
    1. Build the app with PDF feature:
       cargo build --lib --features pdf
    
    2. Run OCR test with real PDF:
       cargo test ocr::tests::test_ocr_extraction -- --nocapture
    
    3. Manual test (if automated tests pass):
       - Launch the app
       - Import a PDF with known text (e.g., a page from a book)
       - Click "Convert" button on the book card
       - Wait for OCR to complete
       - Open the reader view
       - Verify: Text appears in reflow mode (not empty)
       - Verify: Extracted text matches the PDF content
    
    4. Check extraction quality:
       - Compare OCR output with original PDF text
       - Note any errors or missing characters
       - Test with both Japanese and English pages
    
    Expected outcome:
    - OCR extracts actual text (not empty strings)
    - Text is readable in the reflow reader
    - Confidence scores reflect quality (high confidence = accurate text)
  </how-to-verify>
  <verify>
    <automated>cargo test ocr::tests::test_ocr_extraction -- --nocapture && echo "AUTOMATED_TEST_PASSED"</automated>
  </verify>
  <done>Human confirms OCR extracts readable text from actual PDF pages</done>
  <resume-signal>Type "verified" if OCR extracts real text, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- ONNX models present in assets/ocr/models/
- NdlocrEngine loads both detection and recognition models
- postprocess_onnx_output() returns actual text lines (not empty vectors)
- OCR inference extracts text from real PDF page images
- Markdown formatting preserves document structure
- Confidence scores are calculated and returned
- Integration tests pass with real PDF pages
- Human verification confirms text extraction works in app
</verification>

<success_criteria>
1. Models bundled: assets/ocr/models/text_detection.onnx and text_recognition.onnx exist
2. Model loading: NdlocrEngine::new() creates sessions without errors
3. Postprocessing fixed: postprocess_onnx_output() returns (Vec<String>, Vec<f32>) with actual data
4. Text extraction: process_image() returns non-empty OcrResult.markdown and OcrResult.plain_text
5. Accuracy: OCR extracts 90%+ of text from clear PDF pages (manual verification)
6. Performance: Inference completes in < 2 seconds per page
7. Tests pass: cargo test ocr::tests passes
8. Human verification: User confirms text appears in reader after conversion
</success_criteria>

<output>
After completion, create `.planning/phases/03-pdf-support/03-06-SUMMARY.md`
</output>
