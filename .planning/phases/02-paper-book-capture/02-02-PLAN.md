---
phase: 02-paper-book-capture
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/core/db.rs, src/ui/camera.rs, src/app.rs]
autonomous: true
requirements: [PAPER-01, PAPER-04, PAPER-05]

must_haves:
  truths:
    - "User can capture a book page with camera"
    - "OCR text is saved to database linked to the page image"
    - "User can view both the captured image and extracted text together"
  artifacts:
    - path: "src/core/db.rs"
      provides: "save_page method for database persistence"
      exports: ["save_page", "get_page", "get_pages_by_book"]
    - path: "src/ui/camera.rs"
      provides: "Complete camera → OCR → save flow"
      contains: "run_ocr handler calling OCR engine"
    - path: "src/app.rs"
      provides: "Route for viewing captured pages"
      exports: ["Route::PageView"]
  key_links:
    - from: "src/ui/camera.rs"
      to: "src/core/ocr/engine.rs"
      via: "OcrEngine::process_image call"
      pattern: "ocr_engine\\.process_image"
    - from: "src/ui/camera.rs"
      to: "src/core/db.rs"
      via: "db.save_page call after OCR"
      pattern: "db\\.save_page"
    - from: "src/core/db.rs"
      to: "book_pages table"
      via: "SQL INSERT"
      pattern: "INSERT INTO book_pages"
---

<objective>
Integrate camera UI with OCR engine and database persistence

Purpose: Complete the end-to-end camera → OCR → save workflow, enabling users to capture physical book pages and store them with extracted text

Output: Working camera capture flow with OCR integration and database persistence
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-paper-book-capture/02-CONTEXT.md
@src/ui/camera.rs
@src/core/db.rs
@src/app.rs
</context>

<interfaces>
<!-- Key types from existing code -->

From src/core/db.rs:
```rust
pub struct Database {
    conn: Connection,
}

impl Database {
    pub fn open(path: impl AsRef<Path>) -> Result<Self>;
    pub fn create_sticky_note(&self, note: &NewStickyNote) -> Result<i64>;
}
```

From src/ui/camera.rs:
```rust
#[component]
pub fn CameraPage() -> Element {
    // Current state: captured_image, ocr_result, is_processing
}
```

From src/platform/android.rs:
```rust
pub struct CameraResult {
    pub image_data: Vec<u8>,
    pub width: u32,
    pub height: u32,
    pub format: String,
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: Add page database methods</name>
  <files>src/core/db.rs</files>
  <action>
    Extend Database with page-specific methods:
    
    1. Add `save_page` method:
       ```rust
       pub fn save_page(&self, page: &NewBookPage) -> Result<i64>
       ```
       Inserts into book_pages table with: book_id, page_number, image_path, ocr_markdown, ocr_text_plain, confidence
    
    2. Add `get_page` method:
       ```rust
       pub fn get_page(&self, id: i64) -> Result<Option<BookPage>>
       ```
    
    3. Add `get_pages_by_book` method:
       ```rust
       pub fn get_pages_by_book(&self, book_id: &str) -> Result<Vec<BookPage>>
       ```
       Returns all pages for a book, ordered by page_number
    
    4. Add BookPage and NewBookPage structs (similar to StickyNote pattern)
    
    Note: book_pages table already exists in schema from Phase 1
  </action>
  <verify>
    <automated>cargo test db::tests::pages_crud -- --nocapture</automated>
  </verify>
  <done>
    - save_page inserts page and returns ID
    - get_page retrieves by ID
    - get_pages_by_book returns sorted pages
    - All methods have unit tests (minimum 4 tests)
  </done>
</task>

<task type="auto">
  <name>task 2: Connect camera UI to OCR engine</name>
  <files>src/ui/camera.rs</files>
  <action>
    Replace TODO in camera.rs run_ocr handler:
    
    1. Import OcrEngine trait and NdlocrEngine
    2. In run_ocr closure:
       - Get captured image data
       - Create/initialize OCR engine (lazy init or singleton pattern)
       - Call `ocr_engine.process_image(&image_data).await`
       - Handle errors gracefully (show error message, allow retry)
       - Set ocr_result with extracted text (markdown or plain_text)
    
    3. Add loading state during OCR processing (already has is_processing)
    4. Add error state for OCR failures
    
    Pattern: Use lazy_static or once_cell for OCR engine singleton to avoid reloading models
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>
    - run_ocr handler calls actual OCR engine
    - OCR result displays in UI
    - Error handling shows user-friendly messages
    - Loading state visible during processing
  </done>
</task>

<task type="auto">
  <name>task 3: Add save functionality after OCR</name>
  <files>src/ui/camera.rs</files>
  <action>
    Implement "Save" button in camera UI (currently has TODO comment):
    
    1. Add book_id state to CameraPage (prop from route or selection)
    2. In save handler:
       - Call storage.save_page_image(&image_data, &book_id)
       - Create NewBookPage with image_path, ocr_markdown, ocr_text_plain
       - Call db.save_page(&new_page)
       - Show success message or navigate to page view
    3. Add page_number extraction or manual input (per CONTEXT.md: OCR auto-extract with manual fallback)
    
    UI enhancement: Show detected page number from OCR (if confident) with edit capability
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>
    - Save button persists page to database
    - Image saved to filesystem, path in database
    - OCR text saved in both markdown and plain_text formats
    - User sees confirmation after save
  </done>
</task>

<task type="auto">
  <name>task 4: Create page viewer component</name>
  <files>src/ui/page_view.rs, src/app.rs</files>
  <action>
    Create new PageView component to display captured pages:
    
    1. Create src/ui/page_view.rs with PageView component:
       - Accept page_id or book_id as prop
       - Fetch page(s) from database
       - Display image (load from filesystem path)
       - Display OCR text (markdown rendered or plain text)
       - Show page number, confidence score
    
    2. Add Route::PageView { id: i64 } to app.rs router
    3. Add route handler in main app component
    
    4. Styling: Side-by-side layout (image left, text right) on large screens, stacked on mobile
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>
    - PageView component exists and renders page data
    - Route accessible via navigation
    - Image displays correctly from filesystem
    - OCR text displays with formatting
  </done>
</task>

</tasks>

<verification>
1. End-to-end flow works: Capture → OCR → Save → View
2. Database contains page records with correct data
3. Images stored in filesystem, paths in database
4. All cargo tests pass: `cargo test --lib db::tests::pages`
</verification>

<success_criteria>
- [ ] PAPER-01: Camera capture saves image
- [ ] PAPER-04: OCR text saved to database linked to page
- [ ] PAPER-05: User can view image and text together
- [ ] Complete flow: capture → OCR → save → view works end-to-end
- [ ] Minimum 6 new tests for database methods
</success_criteria>

<output>
After completion, create `.planning/phases/02-paper-book-capture/02-02-SUMMARY.md` with:
- Integration patterns used (OCR engine singleton, lazy init)
- Database method test results
- UI flow description
- Screenshots of camera and page view components
</output>
