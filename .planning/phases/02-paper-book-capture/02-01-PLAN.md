---
phase: 02-paper-book-capture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/core/ocr/engine.rs, src/core/ocr/preprocess.rs, src/core/storage.rs]
autonomous: true
requirements: [PAPER-02, PAPER-03]

must_haves:
  truths:
    - "Captured images are downscaled to 2MP or less before processing"
    - "OCR engine processes images and returns extracted text"
    - "OCR processing completes within reasonable time (< 5s on mid-range device)"
  artifacts:
    - path: "src/core/ocr/engine.rs"
      provides: "NdlocrEngine implementation with process_image"
      exports: ["process_image", "is_ready"]
    - path: "src/core/ocr/preprocess.rs"
      provides: "Image downscaling to 2MP max"
      contains: "pub fn preprocess_image"
    - path: "src/core/storage.rs"
      provides: "save_page_image method"
      exports: ["save_page_image"]
  key_links:
    - from: "src/core/ocr/engine.rs"
      to: "tract-onnx runtime"
      via: "model inference"
      pattern: "tract_onnx\\.|\btract\\b"
    - from: "src/core/ocr/preprocess.rs"
      to: "image crate"
      via: "image resizing"
      pattern: "image::(DynamicImage|GenericImageView)"
---

<objective>
Implement OCR engine and image preprocessing pipeline

Purpose: Enable the core camera → OCR workflow by implementing the NDLOCR-Lite engine and ensuring images are properly downscaled for memory efficiency

Output: Working OCR engine with image preprocessing, ready for integration with camera UI
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-paper-book-capture/02-CONTEXT.md
@src/core/ocr/engine.rs
@src/core/ocr/mod.rs
@src/core/storage.rs
</context>

<interfaces>
<!-- Key types and contracts from existing code -->

From src/core/ocr/engine.rs:
```rust
#[async_trait]
pub trait OcrEngine: Send + Sync {
    async fn process_image(&self, image_data: &[u8]) -> Result<OcrResult>;
    fn is_ready(&self) -> bool;
    fn name(&self) -> &'static str;
}

pub struct OcrResult {
    pub markdown: String,
    pub plain_text: String,
    pub regions: Vec<TextRegion>,
    pub confidence: f32,
    pub processing_time_ms: u64,
}
```

From src/core/storage.rs:
```rust
pub struct StorageService {
    pub assets_dir: PathBuf,
}

impl StorageService {
    pub fn save_image(&self, image_data: &[u8], prefix: &str) -> Result<String>;
    pub fn delete_image(&self, path: &str) -> Result<()>;
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: Implement image preprocessing with downscaling</name>
  <files>src/core/ocr/preprocess.rs</files>
  <action>
    Implement `preprocess_image` function that:
    1. Decodes JPEG image data using `image::load_from_memory`
    2. Calculates new dimensions maintaining aspect ratio (max 2MP total pixels)
    3. Resizes using `image::imageops::resize` with Lanczos3 filter
    4. Applies auto-enhancement: convert to grayscale, increase contrast
    5. Returns processed image as JPEG bytes
    
    Formula for downscaling: if width * height > 2_000_000, scale = sqrt(2_000_000 / (width * height))
    
    Use image crate (already in Cargo.toml). Target output: JPEG at 85% quality.
  </action>
  <verify>
    <automated>cargo test ocr::preprocess::tests -- --nocapture</automated>
  </verify>
  <done>
    - preprocess_image function accepts &[u8], returns Result<Vec<u8>>
    - Images > 2MP are downscaled, smaller images pass through
    - Tests verify dimension calculation and output size
  </done>
</task>

<task type="auto" tdd="true">
  <name>task 2: Implement NDLOCR-Lite engine with tract</name>
  <files>src/core/ocr/engine.rs</files>
  <action>
    Replace placeholder in NdlocrEngine::process_image with real implementation:
    
    1. Load ONNX models in initialize() using tract-onnx:
       - text_detection.onnx (DEIMv2)
       - text_recognition.onnx (PARSeq)
       - direction_classifier.onnx (optional)
    
    2. In process_image():
       - Call preprocess_image first
       - Run text detection to find regions
       - Run direction classification on each region
       - Run text recognition on each region
       - Sort regions by reading order (top-to-bottom, right-to-left for vertical)
       - Generate markdown and plain text output
    
    3. Return OcrResult with regions, confidence scores, processing time
    
    Research tract-onnx usage pattern: create model with tract_onnx::onnx(), optimize(), into_runnable()
    
    Model files location: assets/models/ndlocr/ (create this directory structure)
  </action>
  <verify>
    <automated>cargo test ocr::engine::tests -- --nocapture</automated>
  </verify>
  <done>
    - NdlocrEngine::initialize() loads ONNX models successfully
    - NdlocrEngine::process_image() returns OcrResult with markdown and plain_text
    - Processing time < 5s for 2MP images (test with sample image)
    - is_ready() returns true after successful initialization
  </done>
</task>

<task type="auto">
  <name>task 3: Add page image storage method</name>
  <files>src/core/storage.rs</files>
  <action>
    Add `save_page_image` method to StorageService:
    
    ```rust
    pub fn save_page_image(&self, image_data: &[u8], book_id: &str) -> Result<String>
    ```
    
    This method:
    1. Creates directory structure: pages/{book_id}/{timestamp}_{uuid}.jpg
    2. Saves the (already downscaled) image data
    3. Returns relative path for database storage
    
    Reuse existing save_image pattern but organize by book_id for better file management.
  </action>
  <verify>
    <automated>cargo test storage::tests::test_save_page_image -- --nocapture</automated>
  </verify>
  <done>
    - save_page_image method exists and saves images to pages/{book_id}/ directory
    - Returns relative path string
    - Test verifies file exists and path format is correct
  </done>
</task>

</tasks>

<verification>
1. All cargo tests pass: `cargo test --lib ocr -- --nocapture`
2. OCR engine can process a sample image: create integration test with real image
3. Image downscaling verified: 12MP image → < 2MP output
</verification>

<success_criteria>
- [ ] PAPER-02: Image downscaling implemented and tested
- [ ] PAPER-03: OCR engine processes images and returns text
- [ ] Processing time < 5s for 2MP images
- [ ] All unit tests pass (minimum 5 new tests)
- [ ] Integration test with sample book page image
</success_criteria>

<output>
After completion, create `.planning/phases/02-paper-book-capture/02-01-SUMMARY.md` with:
- Implementation details (tract-onnx usage pattern)
- Test results and performance metrics
- Any deviations from plan
- Lessons learned for Phase 3 (PDF support)
</output>
